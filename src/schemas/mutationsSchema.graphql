# import { ItemCart, AddCartRequest, UpdateCartRequest, HttpResponse } from "./cartSchema.graphql"
# import { Checkout, CheckoutRequest } from './checkoutSchema.graphql'

type Mutation {
  addCart(input: AddCartRequest): ItemCart,
  updateItem(input: UpdateCartRequest): HttpResponse,
  removeItem(cartItemId: Int): HttpResponse,
  checkout(input: CheckoutRequest): Checkout
}